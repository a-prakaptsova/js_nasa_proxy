<div>Asteroid Id: {{asteroid.id}}</div>
<div>Asteroid Name: {{asteroid.name}}</div>
<div>Diameter in meters: from {{asteroid.estimated_diameter.meters.estimated_diameter_min}} to {{asteroid.estimated_diameter.meters.estimated_diameter_max}}</div>
<div>Is potentially hazardous asteroid: {{asteroid.is_potentially_hazardous_asteroid}}</div>
<div>Close approach date: 
    {% for data in asteroid.close_approach_data %}
    {{data.close_approach_date_full}}, with relative velocity {{data.relative_velocity.kilometers_per_second}} km/s;
    {% endfor %}
</div>
